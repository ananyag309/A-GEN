function displayWeather(weatherData) {
    // Implement logic to display the current weather data
    // For demonstration purposes, log the weather data to the console
    console.log(weatherData);
}